name: Macos

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  update:
    strategy:
      fail-fast: false
      matrix:
        arch: [amd64, arm64]
        include:
          - arch: arm64
            os: macos-26
          - arch: amd64
            os: macos-26-intel
            
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7

      - name: Setup Node Version
        uses: ./.github/actions/setup-version

      - name: Restore ccache
        id: ccache
        uses: ./.github/actions/restore-ccache
        with:
          arch: ${{ matrix.arch }}

      - name: Install building tools
        run: |
          sudo chown -R $(whoami) $(brew --prefix)/*
          brew untap aws/tap || true
          brew install git ninja nasm ccache llvm

      - name: Build NodeJS
        env:
          ARCH: ${{ matrix.arch }}
        shell: bash
        run: |
          set -o pipefail
          scripts/build.sh 2>&1 | tee build-failure-${{ matrix.arch }}-macos.log

      - name: Create archive
        if: success()
        run: |
          cd node/out/Release
          ln libnode.* libnode.dylib
          ls -lh libnode.* node
          tar -cJf "libnode-${{ matrix.arch }}-macos.tar.xz" libnode.* node

      - name: Handle Release Assets
        if: always()
        uses: ./.github/actions/upload-assets
        with:
          os_name: macos
          arch: ${{ matrix.arch }}
          job_status: ${{ job.status }}
          node_version: ${{ env.NODE_VERSION }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          archive_path: node/out/Release/libnode-${{ matrix.arch }}-macos.tar.xz

      - name: Save ccache
        if: always()
        uses: ./.github/actions/save-ccache
        with:
          cache-hit: ${{ steps.ccache.outputs.cache-hit }}
          primary-key: ${{ steps.ccache.outputs.cache-primary-key }}